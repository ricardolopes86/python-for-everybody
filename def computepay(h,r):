def computepay(h,r):
    if h >= 40:
        pay = h * r
        return pay
    else:
        pay = 40 * r + (r * 1.5 -(h -40))
    
    return pay

in = raw_input("Enter Hours:")
hora = float(in)
in = raw_input("Enter rate:")
rate = float(in)
p = computepay(hora,rate)
print "Pay",p